version: '3.8'

services:
  tarantool:
    image: tarantool/tarantool:3.2
    container_name: kv-tarantool
    ports:
      - "3301:3301"
    volumes:
      - ./docker/init.lua:/opt/tarantool/init.lua
    command: tarantool /opt/tarantool/init.lua
    restart: unless-stopped